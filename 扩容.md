# 链路追踪日志搜集

## opentelemetry

按照traceid取hash后发送到指定的 collector节点
保证同一个traceid 落到同一个 collector节点

## jaeger collector

- ES扩容
- 增加一个中间层，比如kafka

# 日志搜集

## filebeat

每台机器上部署一份，大概率不大可能出现性能问题

## logstash

kibana中观察事件处理延迟，接收事件速率，发送事件速率是否大致一致

## ES

- 监控 429 插入过于频繁导致插入失败的错误
- 在ES中查询，过去5分钟，ES收到的日志时间-logstash接收到的时间>某一个阈值，判断是否有大量日志延迟

解决方案：

- ES扩容
- 在logstash与ES之间插入一个中间件，比如kafka。
- logstash集群

# 指标搜集

## prometheus

- 开启自监控
- 每秒收集多少样本
  rate(prometheus_tsdb_head_samples_appended_total[$__rate_interval])
- 每个样本的平均大小
  (rate(prometheus_tsdb_compaction_chunk_size_bytes_sum[id]))/
  rate(prometheus_tsdb_compaction_chunk_samples_sum[id])
- 每秒产生的数据量 = 每秒收集样本\*每个样本的平均大小
  预估磁盘容量
- 接口请求速率
  histogram_quantile(0.95, sum(rate(prometheus_http_request_duration_seconds_bucket[$__rate_interval])) by (le))

解决方案：
让单独的服务用单独的prometheus节点采集，用同一个grafana展示。
