# 性能分析清单

> 适用于系统性能问题排查，按“现象 -> 定位 -> 证据 -> 结论”的逻辑进行分析。

---

## 1. CPU 和内存问题排查

### 现象判断

- 系统监控发现 CPU 或内存异常
- 服务响应变慢，吞吐下降，机器负载升高
- 进程占用异常，出现长时间高负载

### 定位步骤

1. 先观察系统整体负载
   - `top`：查看 CPU 使用率、内存占用和进程负载
2. 定位异常进程
   - `ps -ef | grep 进程名称`
   - 获取对应 PID
3. 进一步定位代码问题
   - 使用 `pprof` 分析热点代码路径

### 常用命令

```bash
top
ps -ef | grep 进程名称
pprof -seconds 30 -pdf -raw_topprof 10000 10000 > topprof.pdf
```

### 重点判断

- 通过 `pprof` 定位真正引发 CPU 或内存异常的代码段
- 结合火焰图、top profile 或堆分析结果，缩小问题范围

---

## 2. 缓存热点与文件占用分析

### 场景

- 发现系统 IO 或缓存异常
- 想确认是不是某些文件被重复读取或占用过高

### 全局热点

```bash
sudo ./hcache --top 10
```

- 查看全局最大的缓存文件或热点对象

### 指定进程热点

```bash
sudo ./hcache -pid XXXX
```

- 查看某个进程对应的缓存文件占用情况

---

## 3. 网络问题排查

### 关注指标

- 网卡流入流出流量
- 每秒收发包数
- 丢包数
- TCP 重传情况

### 常用命令

```bash
sar -n DEV 1 5
sudo nethogs eth0
```

### 说明

- `sar -n DEV 1 5`：统计网络接口的发包数和流量，连续显示 5 次
- `nethogs`：定位具体哪个进程导致的网络异常

### 结论判断

- 若丢包数急剧上升，说明网络环境可能存在异常
- 若 TCP 重传明显增多，通常意味着网络延迟或链路问题

---

## 4. 磁盘 I/O 检测

### 关注指标

- 读写吞吐
- 设备利用率
- 平均等待时间
- I/O 队列长度

### 常用命令

```bash
iostat -x 1 5
sudo iotop
```

### 说明

- `iostat`：关注磁盘读写和 I/O 负载
- `iotop`：定位哪个进程在大量使用磁盘 I/O

---

## 5. 性能问题的整体排查思路

性能问题通常分为三层：

1. 系统层：CPU、内存、负载、网络、磁盘 I/O
2. 进程层：定位异常进程和资源占用
3. 代码层：通过 `pprof` 找到真正引发瓶颈的代码路径

### 结论判断

- 资源耗尽：优先看 CPU、内存和磁盘
- 连接异常：重点看网络、丢包和 TCP 重传
- 代码热点：通过 `pprof` 验证是否存在热点函数或内存泄漏

---

## 6. 总结

排查性能问题时，建议按照下面顺序：

```text
现象发现 -> 系统指标确认 -> 进程定位 -> 代码分析 -> 结论验证
```

针对不同问题，优先判断是资源耗尽、连接异常，还是代码热点导致的性能退化。
